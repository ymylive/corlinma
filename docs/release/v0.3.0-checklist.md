# Release checklist — v0.3.0

This is the final gate before tagging v0.3.0. Tick each item in review.

## Tests

- [ ] `cargo fmt --all --check` — clean
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` — clean
- [ ] `cargo test --workspace --no-fail-fast` — 687+ pass / 0 fail
- [ ] `uv run pytest python/packages/` — 178+ pass / 2 skipped
- [ ] From `ui/`: `pnpm lint && pnpm typecheck && pnpm test --run` — 23+ passing (2 skipped = known a11y debt)
- [ ] `cargo test -p corlinman-integration-tests --test b5_final` — 5/5 pass
- [ ] `bash scripts/e2e-smoke.sh` — green on Linux + macOS

## Docs

- [ ] `docs/migration/v1-to-v2.md` — published
- [ ] `docs/protocols/nodebridge.md` — published
- [ ] `docs/observability.md` — published
- [ ] `docs/openapi/canvas.yaml` — validates against OpenAPI 3.1
- [ ] `docs/guides/skills-and-agents.md` — reviewed
- [ ] `CHANGELOG.md` — `[0.3.0]` entry groups Batch 1-5 features
- [ ] `README.md` — doc-map includes migration guide + skills/agents guide
- [ ] `ui/README.md` — has "Known a11y debt" section
- [ ] `CREDITS.md` — third-party acknowledgments filled in before public push

## Config & samples

- [ ] `docs/config.example.toml` — every new v2 section commented + documented
- [ ] `skills/`, `agents/`, `TVStxt/{tar,var,sar,fixed}/` — sample files present
- [ ] `corlinman config validate` passes on `docs/config.example.toml`

## Observability

- [ ] Grafana dashboard `ops/dashboards/corlinman.json` — v2, all 15 new counters rendered
- [ ] `/metrics` endpoint exposes: protocol dispatch, wstool invoke/duration, file fetcher bytes, telegram updates/media, hook emits, rate-limit triggers, approval decisions
- [ ] OTLP export smoke-tested locally (follow `docs/observability.md`)

## Protocols

- [ ] Canvas Host endpoints gated by `[canvas] host_endpoint_enabled`; return 503 when off
- [ ] NodeBridge stub server accepts register / dispatches / emits telemetry
- [ ] Block tool protocol (`<<<[TOOL_REQUEST]>>>` + 「始」「末」) parses fuzz corpus 100%
- [ ] Dispatcher dual-track: `openai_function` default, `block` opt-in per agent
- [ ] v5 → v6 vector schema migration is idempotent and backwards-query-compatible
- [ ] Config hot-reload — SIGHUP + fs watcher + `POST /admin/config/reload` all trigger; restart-required sections flagged

## Operational

- [ ] No open P0 / P1 issues
- [ ] Backup restore flow documented for rollback (see migration doc §7)
- [ ] Docker compose bundle builds clean with new crates (`corlinman-hooks`, `corlinman-skills`, `corlinman-wstool`, `corlinman-nodebridge`, `corlinman-tagmemo`)
- [ ] Upgrade path tested on a v0.1.x production snapshot

## Known debt (acceptable)

- `approvals` + `canvas` a11y audit tests skipped in jsdom — documented in `ui/README.md`
- `decider` field on `ApprovalDecided` hook still `None` — will plumb operator identity in a follow-up
- Block-protocol cascade expansion is single-pass (no nested recursion) — tracked in B2-BE6 TODO
- `TVStxt/fixed/` tier is programmatic-only today; disk backing is a future tier

---

When every checkbox is green, tag the release:

```bash
git tag -a v0.3.0 -m "v0.3.0 — batch 1-5"
git push origin v0.3.0
```
